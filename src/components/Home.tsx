import React, { type ReactNode } from "react";

export const Home: React.FC = (): ReactNode => {
    return (
        <div>
            <div className="bg-blue-500 text-white p-4 rounded-lg">¡Tailwind funciona!</div>
        </div>
    )
}